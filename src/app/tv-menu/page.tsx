import TvMenuRuntime from '@/components/tv-menu/TvMenuRuntime';

/** Без идентификатора показывается первая доска — старая ссылка продолжает работать. */
export default function TvMenuPage() {
  return <TvMenuRuntime />;
}
